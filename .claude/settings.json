{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */agent/asyncapi-researcher.md) ./scripts/sync-agent.sh >/dev/null && echo '{\"systemMessage\":\"sync-agent: plugin copy regenerated from agent/asyncapi-researcher.md\"}' ;; */plugins/asyncapi-knowledge/agents/asyncapi-researcher.md) ./scripts/sync-agent.sh >/dev/null && echo '{\"systemMessage\":\"sync-agent: the plugin copy is GENERATED — direct edits are overwritten from agent/asyncapi-researcher.md\"}' ;; esac; }",
            "timeout": 30,
            "statusMessage": "Syncing asyncapi-researcher persona"
          }
        ]
      }
    ]
  }
}
